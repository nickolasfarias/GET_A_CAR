<div class="banner" style="background-image: url(Banner.png); width: 700px; height: 1728px">
  <div class="container">
    <h1>Prime brings the best luxury cars for you!</h1>
  </div>
</div>

<div class="container">
  <div class="cards mt-3">
    <% @cars.each do |car| %>
      <div class="card-trip">
        <%= cl_image_tag car.photo.key, height: 300, width: 400, crop: :fill %>
        <div class="card-trip-infos">
          <div>
            <h2><%= car.model %></h2>
          </div>
          <h2 class="card-trip-pricing">$<%= car.price %>/day</h2>
          <%= link_to "Details", car_path(car), class: "card-button" %>
          <%= cl_image_tag car.user.photo.key, class: "card-trip-user avatar-bordered", crop: :fill %>
        </div>
      </div>
    <% end %>
  </div>
</div>
